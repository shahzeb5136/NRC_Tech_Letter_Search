# secrets - never commit
.env
.streamlit/secrets.toml

# generated index and audit trail (leading slash = project root only, so that the
# nrc_rag/index and nrc_rag/audit source packages are still tracked)
/index/
/audit/
/index_test_tmp/
*.log

# python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
*.egg-info/
.venv/
venv/

# OS / editors / Google Drive artifacts
desktop.ini
Thumbs.db
.DS_Store
~$*
.vscode/
.idea/

# v1 prototype originals in the project root still contain hard-coded keys.
# Redacted copies are tracked in legacy/ instead.
/01_query.py
/02_dashboard.py
/Flow diags.txt
